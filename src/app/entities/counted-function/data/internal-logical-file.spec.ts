import { InternalLogicalFile } from './internal-logical-file';

describe('InternalLogicalFile', () => {
  it('should create an instance', () => {
    expect(new InternalLogicalFile()).toBeTruthy();
  });
});
