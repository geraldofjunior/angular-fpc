import { LocalStorageProvider } from './local-storage-provider';

describe('LocalStorageProvider', () => {
  it('should create an instance', () => {
    expect(new LocalStorageProvider()).toBeTruthy();
  });
});
